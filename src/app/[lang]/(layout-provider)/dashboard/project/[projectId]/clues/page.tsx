import { Card, CardContent, CardHeader, CardTitle } from "#/components";

export default function ProjectCluesPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>资产线索</CardTitle>
            </CardHeader>
            <CardContent>
                <p className="text-muted-foreground">资产线索页面内容将在此处显示</p>
            </CardContent>
        </Card>
    );
}
