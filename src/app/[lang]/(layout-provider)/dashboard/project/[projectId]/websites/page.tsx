import { Card, CardContent, CardHeader, CardTitle } from "#/components/ui/card";

export default function ProjectWebsitesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>网站列表</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">网站列表页面内容将在此处显示</p>
      </CardContent>
    </Card>
  );
}
