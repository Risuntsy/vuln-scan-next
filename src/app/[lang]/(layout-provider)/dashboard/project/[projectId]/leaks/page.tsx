import { Card, CardContent, CardHeader, CardTitle } from "#/components/ui/card";

export default function ProjectLeaksPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>信息泄露</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">信息泄露页面内容将在此处显示</p>
      </CardContent>
    </Card>
  );
}
